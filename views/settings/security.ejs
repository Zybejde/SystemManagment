<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Security Settings</title>
    <!-- Bootstrap styles -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <!-- Importing custom CSS -->
    <link rel="stylesheet" href="/public/styles.css">
</head>
<body>
    <!-- Security Settings container -->
    <div class="container mt-5">
        <h2>Security Settings</h2>
        <br>

        <!-- Change Password Form -->
        <div class="card mb-4">
            <div class="card-header">
                <h5>Change Password</h5>
            </div>
            <div class="card-body">
                <form action="/inventory/settings/update-password" method="POST">
                    <div class="mb-3">
                        <label for="current-password" class="form-label">Current Password</label>
                        <input type="password" name="current-password" id="current-password" class="form-control" required>
                    </div>
                    <div class="mb-3">
                        <label for="new-password" class="form-label">New Password</label>
                        <input type="password" name="new-password" id="new-password" class="form-control" required>
                    </div>
                    <div class="mb-3">
                        <label for="confirm-password" class="form-label">Confirm New Password</label>
                        <input type="password" name="confirm-password" id="confirm-password" class="form-control" required>
                    </div>
                    <button type="submit" class="btn btn-primary">Change Password</button>
                </form>
            </div>
        </div>

        <!-- Enable/Disable Two-Factor Authentication (2FA) -->
        <div class="card mb-4">
            <div class="card-header">
                <h5>Two-Factor Authentication</h5>
            </div>
            <div class="card-body">
                <p>Enable Two-Factor Authentication for added security on your account.</p>
                <form action="/inventory/settings/toggle-2fa" method="POST">
                    <button type="submit" class="btn btn-warning">
                        <%= user.twoFactorEnabled ? 'Disable 2FA' : 'Enable 2FA' %>
                    </button>
                </form>
            </div>
        </div>

        <!-- Security Logs Section -->
        <div class="card mb-4">
            <div class="card-header">
                <h5>Security Logs</h5>
            </div>
            <div class="card-body">
                <p>View recent login attempts and security events:</p>
               
            </div>
        </div>

        <!-- Log Out All Devices -->
        <div class="card mb-4">
            <div class="card-header">
                <h5>Logout From All Devices</h5>
            </div>
            <div class="card-body">
                <p>If you suspect unauthorized access to your account, you can log out of all devices.</p>
                <form action="/inventory/settings/logout-all" method="POST">
                    <button type="submit" class="btn btn-danger">Logout From All Devices</button>
                </form>
            </div>
        </div>

        <!-- Session Management (Auto logout) -->
        <div class="card mb-4">
            <div class="card-header">
                <h5>Session Management</h5>
            </div>
            <div class="card-body">
                <p>Manage your session timeout settings for automatic logout after inactivity.</p>
                <form action="/inventory/settings/update-session-timeout" method="POST">
                    <div class="mb-3">
                        <label for="session-timeout" class="form-label">Session Timeout (in minutes)</label>
                        <input type="number" name="session-timeout" id="session-timeout" class="form-control" value="<%= user.sessionTimeout || 30 %>" required>
                    </div>
                    <button type="submit" class="btn btn-primary">Update Session Timeout</button>
                </form>
            </div>
        </div>

    </div>

    <!-- Bootstrap JS -->
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
